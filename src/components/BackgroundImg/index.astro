---
const { alt, name } = Astro.props;
---

<picture class="backgroung-img">
  <source
    srcset={`/assets/${name}/background-${name}-mobile.jpg`}
    media="(max-width: 375px)"
  />
  <source
    srcset={`/assets/${name}/background-${name}-tablet.jpg`}
    media="(max-width: 768px)"
  />
  <img
    src={`/assets/${name}/background-${name}-desktop.jpg`}
    alt={`${alt} background image`}
  />
</picture>

<style>
  .backgroung-img {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    min-height: 100vh;
    z-index: -1;
  }
  .backgroung-img img {
    width: inherit;
    height: 100%;
    object-fit: cover;
  }
</style>
